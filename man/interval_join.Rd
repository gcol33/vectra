% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/joins.R
\name{interval_join}
\alias{interval_join}
\title{Interval (range overlap) join of two vectra tables}
\usage{
interval_join(
  x,
  y,
  start,
  end,
  by = NULL,
  type = c("inner", "left"),
  closed = TRUE,
  n_threads = 4L,
  suffix = ".y"
)
}
\arguments{
\item{x}{A \code{vectra_node} (the streamed / probe side).}

\item{y}{A \code{vectra_node} (the resident / build side).}

\item{start}{The interval start columns, as \code{c("x_col" = "y_col")} or a
single name shared by both sides.}

\item{end}{The interval end columns, in the same form as \code{start}.}

\item{by}{Optional equality key restricting overlap to rows that match on
it, as \code{c("x_col" = "y_col")} or a single shared name. \code{NULL} (default)
tests every pair.}

\item{type}{\code{"inner"} (default) keeps only overlapping pairs; \code{"left"}
keeps every \code{x} row, filling \code{y} columns with \code{NA} where nothing overlaps.}

\item{closed}{Logical. \code{TRUE} (default) counts intervals that touch at an
endpoint as overlapping; \code{FALSE} requires a strictly positive overlap.}

\item{n_threads}{Integer. OpenMP threads for the per-group sweep. Default \code{4L}.}

\item{suffix}{Character. Suffix appended to \code{y} column names that collide
with \code{x} names. Default \code{".y"}.}
}
\value{
A \code{vectra_node} with all \code{x} columns followed by all \code{y} columns
(suffixed on collision).
}
\description{
Joins each row of \code{x} to every row of \code{y} whose \verb{[start, end]} interval
overlaps it -- the one-dimensional analogue of a spatial bounding-box join
(\code{data.table::foverlaps}, \code{GenomicRanges::findOverlaps}). An optional
equality key in \code{by} restricts overlap testing to rows that share that key
(a chromosome, a sensor id), the same role a regular join's \code{by} plays.
}
\details{
Both sides are read into memory, then within each \code{by} group a sweep over
the interval endpoints emits each overlapping pair once, scaling with the
number of overlaps rather than the product of the row counts.
}
