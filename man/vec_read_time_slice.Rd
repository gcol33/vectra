% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/raster.R
\name{vec_read_time_slice}
\alias{vec_read_time_slice}
\title{Read a single time slice from a .vec time cube}
\usage{
vec_read_time_slice(r, time, band = 1L, level = 0L, cols = NULL, rows = NULL)
}
\arguments{
\item{r}{A \code{vectra_raster} from \code{vec_open_raster()}.}

\item{time}{Time value to match (numeric/integer).}

\item{band}{Band index (1-based).}

\item{level}{Overview level. Default 0.}

\item{cols, rows}{1-based ranges, same as \code{vec_read_window}.}
}
\value{
A numeric matrix.
}
\description{
Performs a linear scan of the index for tiles with \code{time == time} and
decodes the matching window. The lookup is O(n_tiles) per call — Phase
6's optimized hash-map lookup is a follow-up.
}
