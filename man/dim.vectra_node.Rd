% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/dim.R
\name{dim.vectra_node}
\alias{dim.vectra_node}
\title{Dimensions of a lazy query}
\usage{
\method{dim}{vectra_node}(x)
}
\arguments{
\item{x}{A \code{vectra_node} object.}
}
\value{
A length-2 vector \code{c(rows, cols)}, integer unless the row count
exceeds the largest integer R can hold, in which case both are doubles.
\code{rows} is \code{NA} when the count is not derivable from metadata.
}
\description{
Reports the shape of a \code{vectra_node} from plan metadata, without running
the query. Defining \code{dim()} is what makes base R's \code{\link[=nrow]{nrow()}} and \code{\link[=ncol]{ncol()}}
work on a node, since both read \code{dim(x)}.
}
\details{
The column count always comes from the plan's schema. The row count is
available when it can be read from metadata: a \code{.vtr} table reports the
count stored in its row-group index (minus any rows \code{\link[=delete_vtr]{delete_vtr()}} has
tombstoned), and the row-preserving verbs carry it through --
\code{\link[=select]{select()}}, \code{\link[=mutate]{mutate()}}, \code{\link[=rename]{rename()}}, \code{\link[=arrange]{arrange()}}, \code{\link[=relocate]{relocate()}}, window
functions, \code{\link[=head]{head()}}, \code{\link[=slice_head]{slice_head()}}, \code{slice_min()}/\code{slice_max()}, and
\code{\link[=bind_rows]{bind_rows()}} over counted inputs.

Verbs whose output length depends on the data -- \code{\link[=filter]{filter()}}, the joins,
\code{\link[=summarise]{summarise()}}, \code{\link[=distinct]{distinct()}} -- report \code{NA} rows. Counting those means
running the query, which on a larger-than-RAM table is a full pass, so
\code{nrow()} reports what it knows rather than starting one. To get the exact
count, run the query:

\if{html}{\out{<div class="sourceCode r">}}\preformatted{tbl(f) |> filter(x > 0) |> count() |> collect()
}\if{html}{\out{</div>}}

A CSV, SQLite, or TIFF source reports \code{NA} rows too: those formats carry no
row count to read.
}
\examples{
f <- tempfile(fileext = ".vtr")
write_vtr(mtcars, f)

dim(tbl(f))
nrow(tbl(f))                       # 32, straight from the row-group index
ncol(tbl(f) |> select(mpg, cyl))   # 2
nrow(tbl(f) |> head(5))            # 5
nrow(tbl(f) |> filter(cyl == 4))   # NA: needs the query to run

# exact count for a filtered query
tbl(f) |> filter(cyl == 4) |> count() |> collect()

unlink(f)

}
