% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/tbl.R
\name{tbl_bed}
\alias{tbl_bed}
\title{Create a lazy table reference from a BED file}
\usage{
tbl_bed(path, batch_size = .DEFAULT_BATCH_SIZE, quiet = FALSE)
}
\arguments{
\item{path}{Path to a \code{.bed} file, optionally gzip-compressed.}

\item{batch_size}{Number of features per batch (default 65536).}

\item{quiet}{If \code{FALSE} (default), report the feature count when the scan
completes.}
}
\value{
A \code{vectra_node} object representing a lazy scan of the BED file.
}
\description{
Streams a BED (Browser Extensible Data) file of genomic features as rows,
one row per feature. Columns follow the BED specification in order:
\code{chrom}, \code{start}, \code{end}, \code{name}, \code{score}, \code{strand}, \code{thickStart},
\code{thickEnd}, \code{itemRgb}, \code{blockCount}, \code{blockSizes}, \code{blockStarts}; any
fields beyond the twelfth (a "BED N+" file) are read as strings named
\code{V13}, \code{V14}, ... The column count is fixed by the first feature line and
every later line must match it. Features stream one batch at a time, so a
genome-scale interval set never fully materializes. Gzip-compressed files
(\code{.bed.gz}) are read transparently. No data is read until \code{\link[=collect]{collect()}} is
called.
}
\details{
Fields are whitespace-delimited (tab or space), and blank lines, \verb{#}
comments, and UCSC \code{track}/\code{browser} directive lines are skipped. \code{chrom},
\code{start}, and \code{end} are required; \code{start} and \code{end} are integers (a
non-integer there stops the scan). Optional integer fields (\code{score},
\code{thickStart}, \code{thickEnd}, \code{blockCount}) accept \code{.} or \code{NA} as missing.
When the scan reaches the end of the file it reports the number of features
read (suppress with \code{quiet = TRUE}).

\strong{Coordinates are read faithfully.} BED \code{start} is 0-based and \code{end} is
half-open (the first position past the feature), and both are returned
exactly as stored --- no coordinate is rewritten. Two features that abut
(\code{end} of one equal to \code{start} of the next) share no base. To overlap-join
BED tables with those half-open semantics --- matching bedtools and
\code{GenomicRanges::findOverlaps()} --- pass \code{closed = FALSE} to
\code{\link[=interval_join]{interval_join()}}, which requires a strictly positive overlap and so does
not pair abutting features:

\if{html}{\out{<div class="sourceCode r">}}\preformatted{peaks <- tbl_bed("peaks.bed")
genes <- tbl_bed("genes.bed")
interval_join(peaks, genes, start = "start", end = "end",
              by = "chrom", closed = FALSE)
}\if{html}{\out{</div>}}
}
\examples{
f <- tempfile(fileext = ".bed")
writeLines(c("chr1\t100\t200\tfeatA\t0\t+",
             "chr1\t150\t400\tfeatB\t0\t-",
             "chr2\t500\t900\tfeatC\t0\t+"), f)
node <- tbl_bed(f, quiet = TRUE)
node |> filter(chrom == "chr1") |> collect()
unlink(f)

}
\seealso{
\code{\link[=interval_join]{interval_join()}}, \code{\link[=tbl_fasta]{tbl_fasta()}}
}
