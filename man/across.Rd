% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/across.R
\name{across}
\alias{across}
\title{Apply a function across multiple columns}
\usage{
across(.cols, .fns, ..., .names = NULL)
}
\arguments{
\item{.cols}{Column selection (tidyselect).}

\item{.fns}{A function, formula, or named list of functions.}

\item{...}{Additional arguments passed to \code{.fns}.}

\item{.names}{A glue-style naming pattern. Uses \code{{.col}} and \code{{.fn}}.
Default: \code{"{.col}"} if \code{.fns} is a single function,
\code{"{.col}_{.fn}"} if \code{.fns} is a named list.}
}
\value{
A named list used internally by mutate/summarise.
}
\description{
Used inside \code{\link[=mutate]{mutate()}} or \code{\link[=summarise]{summarise()}} to apply a function to multiple
columns selected with tidyselect. Returns a named list of expressions.
}
\examples{
f <- tempfile(fileext = ".vtr")
write_vtr(mtcars, f)
# In summarise (conceptual; across is expanded to individual expressions)
unlink(f)

}
