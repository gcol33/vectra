% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/raster.R
\name{vec_read_window}
\alias{vec_read_window}
\title{Read a window of pixels from a .vec raster}
\usage{
vec_read_window(r, band = 1L, level = 0L, cols = NULL, rows = NULL)
}
\arguments{
\item{r}{A \code{vectra_raster} from \code{vec_open_raster()}.}

\item{band}{Band index (1-based). Default 1.}

\item{level}{Overview level (Phase 1: must be 0).}

\item{cols}{1-based column range \code{c(col_min, col_max)}. Inclusive.
Default \code{c(1, r$width)}.}

\item{rows}{1-based row range \code{c(row_min, row_max)}. Inclusive.
Default \code{c(1, r$height)}.}
}
\value{
A numeric matrix with \code{nrow = row_max - row_min + 1} and
\code{ncol = col_max - col_min + 1}. Nodata pixels become \code{NA}.
}
\description{
Decodes only the tiles overlapping the requested window. Pixels outside
the raster extent come back as \code{NA}.
}
