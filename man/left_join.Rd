% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/joins.R
\name{left_join}
\alias{left_join}
\alias{inner_join}
\alias{right_join}
\alias{full_join}
\alias{semi_join}
\alias{anti_join}
\title{Join two vectra tables}
\usage{
left_join(x, y, by = NULL, suffix = c(".x", ".y"), ...)

inner_join(x, y, by = NULL, suffix = c(".x", ".y"), ...)

right_join(x, y, by = NULL, suffix = c(".x", ".y"), ...)

full_join(x, y, by = NULL, suffix = c(".x", ".y"), ...)

semi_join(x, y, by = NULL, ...)

anti_join(x, y, by = NULL, ...)
}
\arguments{
\item{x}{A \code{vectra_node} object (left table).}

\item{y}{A \code{vectra_node} object (right table).}

\item{by}{A character vector of column names to join by, or a named vector
like \code{c("a" = "b")}. \code{NULL} for natural join (common columns).}

\item{suffix}{A character vector of length 2 for disambiguating non-key
columns with the same name (default \code{c(".x", ".y")}).}

\item{...}{Ignored.}
}
\value{
A \code{vectra_node} with the joined result.
}
\description{
Join two vectra tables
}
\examples{
f1 <- tempfile(fileext = ".vtr")
f2 <- tempfile(fileext = ".vtr")
write_vtr(data.frame(id = c(1, 2, 3), x = c(10, 20, 30)), f1)
write_vtr(data.frame(id = c(1, 2, 4), y = c(100, 200, 400)), f2)
left_join(tbl(f1), tbl(f2), by = "id") |> collect()
unlink(c(f1, f2))

}
