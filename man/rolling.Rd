% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/resample.R
\name{rolling}
\alias{rolling}
\alias{roll_sum}
\alias{roll_mean}
\alias{roll_min}
\alias{roll_max}
\alias{roll_n}
\title{Time-based rolling aggregates}
\usage{
roll_sum(x, time, every)

roll_mean(x, time, every)

roll_min(x, time, every)

roll_max(x, time, every)

roll_n(time, every)
}
\arguments{
\item{x}{Value column to aggregate (not needed for \code{roll_n}).}

\item{time}{Datetime column defining the window.}

\item{every}{Window span as a fixed-width duration string.}
}
\value{
A double column.
}
\description{
Trailing rolling aggregates over a datetime window, usable inside \code{\link[=mutate]{mutate()}}.
For each row, the aggregate covers the rows whose \code{time} falls in
\verb{(time - every, time]} -- the row itself and everything within one window
before it. With an upstream \code{\link[=group_by]{group_by()}}, windows are computed within each
group. \code{NA} values are skipped.
}
\details{
\code{time} must be a \code{Date} or \code{POSIXct} column; \code{every} is a fixed-width
duration string (\code{"15 min"}, \code{"1 hour"}, \code{"7 days"}) -- calendar units
(month, year) are not allowed because their length varies.
}
\examples{
\dontrun{
tbl("readings.vtr") |>
  group_by(sensor) |>
  mutate(avg_1h = roll_mean(temp, t, "1 hour"),
         n_1h   = roll_n(t, "1 hour")) |>
  collect()
}

}
\seealso{
\code{\link[terra:resample]{resample()}}
}
