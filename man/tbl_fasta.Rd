% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/tbl.R
\name{tbl_fasta}
\alias{tbl_fasta}
\title{Create a lazy table reference from a FASTA file}
\usage{
tbl_fasta(path, batch_size = .DEFAULT_BATCH_SIZE, quiet = FALSE)
}
\arguments{
\item{path}{Path to a \code{.fasta}/\code{.fa} file, optionally gzip-compressed.}

\item{batch_size}{Number of records per batch (default 65536).}

\item{quiet}{If \code{FALSE} (default), report the record count when the scan
completes.}
}
\value{
A \code{vectra_node} object representing a lazy scan of the FASTA file.
}
\description{
Streams a FASTA file of biological sequences as rows: one row per record
with columns \code{id}, \code{desc}, and \code{seq}. \code{id} is the first whitespace-delimited
token of the header line, \code{desc} is the remainder (an empty string when the
header carries no description), and \code{seq} is the sequence with line breaks
removed. Records stream one batch at a time, so a read set larger than RAM
never fully materializes. Gzip-compressed files (\code{.fasta.gz}, \code{.fa.gz}) are
read transparently. No data is read until \code{\link[=collect]{collect()}} is called.
}
\details{
The \verb{seq_*} expression family (\code{seq_revcomp()}, \code{seq_gc()}, \code{seq_translate()},
\code{seq_dist()}, ...) operates on the \code{seq} column directly inside
\code{\link[=mutate]{mutate()}}/\code{\link[=filter]{filter()}}. See \link{seq_expressions}.

A record cut short is a loud error, not a silent drop: a header that is not
the first non-blank token, or a byte where a \code{>} is expected, stops the scan.
When the scan reaches the end of the file it reports the number of records
read (suppress with \code{quiet = TRUE}).
}
\examples{
f <- tempfile(fileext = ".fasta")
writeLines(c(">seq1 first", "ACGTACGT", ">seq2 second", "GGGGCCCC"), f)
node <- tbl_fasta(f, quiet = TRUE)
node |> mutate(gc = seq_gc(seq)) |> collect()
unlink(f)

}
\seealso{
\code{\link[=tbl_fastq]{tbl_fastq()}}, \link{seq_expressions}
}
