% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/embedding.R
\name{as_embedding}
\alias{as_embedding}
\title{Encode vectors as an embedding column}
\usage{
as_embedding(x)
}
\arguments{
\item{x}{A numeric matrix (one embedding per row), a list of equal-length
numeric vectors, or a single numeric vector (one embedding).}
}
\value{
A character vector with one hex-encoded blob per embedding.
}
\description{
Packs numeric vectors into the hex float32 blobs vectra stores embedding
columns as. Write the result as an ordinary character column; the distance
functions \code{\link[=cosine]{cosine()}}, \code{\link[=l2]{l2()}}, and \code{\link[=dot]{dot()}} decode it inside the engine.
}
\examples{
m <- matrix(rnorm(12), nrow = 3)        # 3 embeddings of length 4
emb <- as_embedding(m)
df <- data.frame(id = 1:3, emb = emb)

}
\seealso{
\code{\link[=cosine]{cosine()}}, \code{\link[=l2]{l2()}}, \code{\link[=dot]{dot()}}
}
