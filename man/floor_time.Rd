% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/resample.R
\name{floor_time}
\alias{floor_time}
\title{Floor a datetime column to a calendar grid}
\usage{
floor_time(t, unit)
}
\arguments{
\item{t}{A \code{Date} or \code{POSIXct} column (stored as days / seconds since the
epoch).}

\item{unit}{A bucket size as a string: a count and a unit, e.g. \code{"hour"},
\code{"15 min"}, \code{"day"}, \code{"week"}, \code{"3 months"}, \code{"quarter"}, \code{"year"}.}
}
\value{
A numeric (epoch) column.
}
\description{
Truncates a \code{Date} or \code{POSIXct} column down to a unit boundary -- the basis
for time bucketing. Usable inside \code{\link[=mutate]{mutate()}} / \code{\link[=filter]{filter()}}; \code{\link[terra:resample]{resample()}}
calls it for you.
}
\details{
The result is returned in the column's own scale (days for \code{Date}, seconds
for \code{POSIXct}), so it remains a valid instant. The engine does not re-attach
the \code{Date}/\code{POSIXct} class to a computed column, so a floored column
collects as the underlying numeric epoch; wrap with \code{as.POSIXct(x, origin = "1970-01-01", tz = "UTC")} or \code{as.Date(x, origin = "1970-01-01")} if you
need the class back.
}
\seealso{
\code{\link[terra:resample]{resample()}}
}
