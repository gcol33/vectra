% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/embedding.R
\name{embedding_distance}
\alias{embedding_distance}
\alias{cosine}
\alias{l2}
\alias{dot}
\title{Embedding distance functions}
\usage{
cosine(x, y)

l2(x, y)

dot(x, y)
}
\arguments{
\item{x}{An embedding column.}

\item{y}{An embedding column, or a constant numeric query vector.}
}
\value{
A double column.
}
\description{
Distance and similarity functions over embedding columns (see
\code{\link[=as_embedding]{as_embedding()}}), usable inside \code{\link[=mutate]{mutate()}} / \code{\link[=filter]{filter()}} like any other
expression. The query side is either another embedding column or a constant
numeric vector. Decoding and the arithmetic run in C, one row at a time,
parallelized over rows.
}
\details{
\code{cosine()} returns cosine distance (\code{1 - similarity}) and \code{l2()} Euclidean
distance, so smaller means nearer -- pair them with \code{slice_min()} for
nearest-neighbour search. \code{dot()} returns the inner product, where larger
means nearer (\code{slice_max()}).
}
\examples{
\dontrun{
q <- rnorm(128)
tbl("vecs.vtr") |>
  mutate(d = cosine(emb, q)) |>
  slice_min(d, n = 10) |>
  collect()
}

}
\seealso{
\code{\link[=as_embedding]{as_embedding()}}
}
