% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/resample.R
\name{resample}
\alias{resample}
\title{Resample a time series to a calendar grid}
\usage{
resample(.data, time, every, ..., .name = NULL)
}
\arguments{
\item{.data}{A \code{vectra_node}.}

\item{time}{The datetime column to bucket (unquoted).}

\item{every}{Bucket size as a string, e.g. \code{"1 hour"}, \code{"15 min"}, \code{"day"},
\code{"month"} (see \code{\link[=floor_time]{floor_time()}}).}

\item{...}{Named aggregation expressions, as in \code{\link[=summarise]{summarise()}} (e.g.
\code{mean_temp = mean(temp)}).}

\item{.name}{Optional name for the bucket column. Defaults to the name of
\code{time} (the original column, replaced by its floored value).}
}
\value{
A \code{vectra_node} with one row per occupied bucket: the bucket column
followed by the aggregates. The bucket collects as a numeric epoch value
(see \code{\link[=floor_time]{floor_time()}} on restoring the date class).
}
\description{
Buckets a datetime column to a grid (\code{every}) and aggregates within each
bucket -- the time-series form of \code{group_by()} + \code{summarise()}. Equivalent
to \verb{mutate(<time> = floor_time(<time>, every))}, \verb{group_by(<time>)}, then
\code{summarise(...)}.
}
\examples{
\dontrun{
tbl("readings.vtr") |>
  resample(timestamp, "1 hour", mean_temp = mean(temp), n = n()) |>
  collect()
}

}
\seealso{
\code{\link[=floor_time]{floor_time()}}, \code{\link[=summarise]{summarise()}}
}
