% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/tbl.R
\name{tbl_fastq}
\alias{tbl_fastq}
\title{Create a lazy table reference from a FASTQ file}
\usage{
tbl_fastq(path, batch_size = .DEFAULT_BATCH_SIZE, quiet = FALSE)
}
\arguments{
\item{path}{Path to a \code{.fastq}/\code{.fq} file, optionally gzip-compressed.}

\item{batch_size}{Number of records per batch (default 65536).}

\item{quiet}{If \code{FALSE} (default), report the record count when the scan
completes.}
}
\value{
A \code{vectra_node} object representing a lazy scan of the FASTQ file.
}
\description{
Streams a FASTQ file as rows: one row per record with columns \code{id}, \code{desc},
\code{seq}, and \code{qual} (the raw quality string, same length as \code{seq}). \code{id} and
\code{desc} are split from the header as in \code{\link[=tbl_fasta]{tbl_fasta()}}. Records stream one
batch at a time, so a read set larger than RAM never fully materializes.
Gzip-compressed files (\code{.fastq.gz}, \code{.fq.gz}) are read transparently. No data
is read until \code{\link[=collect]{collect()}} is called.
}
\details{
Records are parsed in the standard four-line form (header, sequence, \code{+}
separator, quality). A record cut short --- a missing sequence, separator, or
quality line, or a quality string whose length does not match the sequence
--- is a loud error, not a silent drop. When the scan reaches the end of the
file it reports the number of records read (suppress with \code{quiet = TRUE}).
}
\examples{
f <- tempfile(fileext = ".fastq")
writeLines(c("@r1 read one", "ACGT", "+", "IIII",
             "@r2 read two", "GGCC", "+", "!!!!"), f)
node <- tbl_fastq(f, quiet = TRUE)
node |> mutate(len = seq_length(seq)) |> collect()
unlink(f)

}
\seealso{
\code{\link[=tbl_fasta]{tbl_fasta()}}, \link{seq_expressions}
}
