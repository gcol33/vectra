% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/raster.R
\name{vec_build_overviews}
\alias{vec_build_overviews}
\title{Build overview pyramids for a .vec raster}
\usage{
vec_build_overviews(
  path,
  levels,
  resampling = c("average", "nearest", "bilinear", "mode", "gauss"),
  compression = c("fast", "balanced", "max")
)
}
\arguments{
\item{path}{Path to a \code{.vec} raster file. The file is modified in place.}

\item{levels}{Total levels including level 0 (so \code{levels = 5} adds
four overviews: levels 1..4). Must be in \verb{[2, 16]}.}

\item{resampling}{One of \code{"nearest"}, \code{"average"}, \code{"bilinear"},
\code{"mode"}, \code{"gauss"}. \code{"average"} is the right choice for continuous
rasters; \code{"mode"} for categorical/land-cover.}

\item{compression}{Compression effort for the new tiles. Defaults to
\code{"fast"} because overview tiles are usually one-shot writes.}
}
\value{
Invisible \code{NULL}.
}
\description{
Appends \code{n_levels - 1} reduced-resolution copies of the raster to the
file. Each level is computed by 2x downsampling the previous level
with the chosen kernel. Reading via \code{vec_read_window(level = L)}
picks tiles at level L; the file's \code{n_levels} is updated in place.
}
