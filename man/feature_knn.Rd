% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/feature_knn.R
\name{feature_knn}
\alias{feature_knn}
\title{Nearest neighbours of a streamed layer in predictor space}
\usage{
feature_knn(
  x,
  y,
  vars = NULL,
  k = NULL,
  percentage = NULL,
  metric = c("euclidean", "mahalanobis"),
  dist_col = "knn_distance",
  nthreads = NULL,
  flush_rows = NULL
)
}
\arguments{
\item{x}{A \code{vectra_node} (from \code{\link[=tbl]{tbl()}}, \code{\link[=tbl_csv]{tbl_csv()}}, ...): the streamed query
side. Its predictor columns are read one batch at a time.}

\item{y}{A data.frame or numeric matrix: the resident reference cloud, one
column per predictor. Rows with any \code{NA} are dropped.}

\item{vars}{Character vector of predictor column names present in both \code{x} and
\code{y}. Defaults to the column names of \code{y}.}

\item{k}{Number of nearest neighbours to average over. Supply exactly one of
\code{k} or \code{percentage}.}

\item{percentage}{Percent of the reference cloud to average over (the nearest
\code{ceil(percentage/100 * nrow(y))} neighbours). Supply exactly one of \code{k} or
\code{percentage}.}

\item{metric}{\code{"euclidean"} (default) for straight L2 distance in predictor
units, or \code{"mahalanobis"} for distance whitened by the reference cloud's
covariance.}

\item{dist_col}{Name of the appended distance column. Default
\code{"knn_distance"}.}

\item{nthreads}{Threads for the per-batch distance scan. \code{NULL} (default) uses
all available (capped to two under \verb{R CMD check}).}

\item{flush_rows}{Rows to buffer before spilling a run file. \code{NULL} (default)
spills by the streaming memory budget instead.}
}
\value{
A \code{vectra_node} of \code{x}'s columns plus \code{dist_col}, backed by temporary
\code{.vtr} spills removed when the node is garbage-collected.
}
\description{
Streams a query side \code{x} through the engine and, for each row, returns the
mean distance to its nearest neighbours in a resident reference cloud \code{y} --
where distance is measured in \emph{predictor} (attribute) space, not on
coordinates. This is the continuous half of the MOP transferability metric
(Owens et al. 2013): the mean environmental distance from a projection cell to
the nearest part of a calibration cloud. Where \code{\link[=spatial_knn]{spatial_knn()}} measures
geographic proximity with GEOS, \code{feature_knn()} measures environmental
novelty with an L2 or Mahalanobis distance in n-variable space.
}
\details{
The reference cloud is materialized once, whitened for the chosen metric, and
held resident; the query side streams one batch at a time, so the projection
side can exceed memory while peak memory stays at one batch plus the resident
cloud. The neighbour count is either an absolute \code{k} or the nearest
\code{percentage}\% of the cloud (\code{ceil(percentage/100 * nrow(y))}), matching MOP's
proportion-of-reference formulation.
}
\examples{
set.seed(1)
ref <- data.frame(bio1 = rnorm(500, 10, 2), bio12 = rnorm(500, 800, 50))
qy  <- data.frame(bio1 = c(10, 20), bio12 = c(800, 400))
f <- tempfile(fileext = ".vtr")
write_vtr(qy, f)

# Row 1 sits in the cloud (small distance); row 2 is far outside it.
tbl(f) |>
  feature_knn(ref, percentage = 5) |>
  collect()
unlink(f)

}
\references{
Owens, H.L. et al. (2013) Constraints on interpretation of
ecological niche models by limited environmental ranges on calibration
areas. \emph{Ecological Modelling} 263:10-18.
}
\seealso{
\code{\link[=mop]{mop()}} for the raster transferability surface built on this
primitive, \code{\link[=spatial_knn]{spatial_knn()}} for geographic nearest neighbours.
}
