% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/spatial.R
\name{spatial_overlay}
\alias{spatial_overlay}
\title{Self-overlay a polygon layer into disjoint pieces (QGIS-style Union)}
\usage{
spatial_overlay(
  x,
  vars = NULL,
  piece = "piece_id",
  geom = "geometry",
  flush_rows = NULL,
  mem_limit = NULL,
  threads = NULL,
  quiet = TRUE
)
}
\arguments{
\item{x}{An \code{sf} object with polygon or multipolygon geometry.}

\item{vars}{Character vector of attribute columns of \code{x} to carry onto each
piece. Default \code{NULL} keeps them all; name a subset to keep the streamed
output narrow.}

\item{piece}{Name of the integer piece-id column added to the output (the key
you group by to resolve overlaps). Default \code{"piece_id"}.}

\item{geom}{Name of the output hex-WKB geometry column. Default \code{"geometry"}.}

\item{flush_rows}{Exploded rows buffered before a spill flush. Defaults to
\code{getOption("vectra.spatial_flush", 5e5)}.}

\item{mem_limit}{Approximate peak working-set budget in bytes. Components are
grouped into chunks within this budget and each chunk is overlaid then
spilled before the next, so memory stays bounded regardless of layer size.
Raise it for more parallel throughput, lower it for tighter memory. Defaults
to \code{getOption("vectra.overlay_mem_limit", 2e9)}.}

\item{threads}{Number of OpenMP threads for the per-component overlay within a
chunk. \code{0} (the default, via \code{getOption("vectra.overlay_threads", 0)}) uses
all available cores.}

\item{quiet}{If \code{FALSE}, show a text progress bar over the overlay chunks.}
}
\value{
A \code{vectra_node} over the exploded overlay (one row per piece per
covering polygon), backed by temporary \code{.vtr} spills removed when the node
is garbage-collected, carrying the CRS of \code{x} for \code{\link[=collect_sf]{collect_sf()}}.
}
\description{
Splits a polygon layer along all its own overlaps into disjoint pieces and
returns a lazy node with one row per piece per covering polygon: where \code{k}
polygons overlap, that piece appears \code{k} times, each row carrying one source
polygon's attributes. This is the union overlay GIS tools expose as
"Union (single layer)", with the overlap retained once per contributing
feature rather than dissolved. Resolve the duplicates with a grouped
\code{\link[=slice_min]{slice_min()}} / \code{\link[=slice_max]{slice_max()}} -- for example earliest designation year wins:
\code{group_by(piece_id) |> slice_min(year)}.
}
\details{
The topology is done once with \pkg{sf}/GEOS and tiled over connected overlap
clusters (disjoint clusters never share a piece, so the tiling is exact and
bounded in memory), then the exploded pieces are streamed to a \code{.vtr} and
handed back as a lazy node. Geometry rides through the engine as hex-encoded
WKB in a string column; the CRS is carried on the node for \code{\link[=collect_sf]{collect_sf()}}.

The overlay runs on a fixed-precision model: coordinates are snapped to a
grid derived from their own magnitude so the pieces come out disjoint and
their areas reconstruct the union of the inputs, instead of drifting by the
fraction of a percent that floating-point sliver artefacts on invalid input
otherwise introduce. Inputs are also passed through \code{\link[sf:st_make_valid]{sf::st_make_valid()}}.

The input \code{x} must be a resident \code{sf} object: building the overlap graph and
intersecting needs the geometries in memory. The exploded result, which is
typically several times larger, is what streams to disk.
}
\examples{
\dontshow{if (requireNamespace("sf", quietly = TRUE)) withAutoprint(\{ # examplesIf}
# Two overlapping squares designated in different years.
sq <- function(a, b) sf::st_polygon(list(rbind(
  c(a, 0), c(b, 0), c(b, 1), c(a, 1), c(a, 0))))
polys <- sf::st_sf(year = c(1990L, 2010L),
                   geometry = sf::st_sfc(sq(0, 2), sq(1, 3)))

# Split into disjoint pieces; earliest year wins where they overlap.
first <- spatial_overlay(polys) |>
  group_by(piece_id) |>
  slice_min(year, n = 1, with_ties = FALSE) |>
  collect_sf()
first
\dontshow{\}) # examplesIf}
}
\seealso{
\code{\link[=slice_min]{slice_min()}} / \code{\link[=slice_max]{slice_max()}} to resolve each piece to one winner,
\code{\link[=collect_sf]{collect_sf()}} to materialize as \code{sf}.
}
