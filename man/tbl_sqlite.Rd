% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/tbl.R
\name{tbl_sqlite}
\alias{tbl_sqlite}
\title{Create a lazy table reference from a SQLite database}
\usage{
tbl_sqlite(path, table, batch_size = 65536L)
}
\arguments{
\item{path}{Path to a SQLite database file.}

\item{table}{Name of the table to scan.}

\item{batch_size}{Number of rows per batch (default 65536).}
}
\value{
A \code{vectra_node} object representing a lazy scan of the table.
}
\description{
Opens a SQLite database and lazily scans a table. Column types are inferred
from declared types in the CREATE TABLE statement. All filtering, grouping,
and aggregation is handled by vectra's C engine — no SQL parsing needed.
No data is read until \code{\link[=collect]{collect()}} is called.
}
\examples{
\dontrun{
node <- tbl_sqlite("data.sqlite", "measurements")
node |> filter(year > 2020) |> collect()
}

}
