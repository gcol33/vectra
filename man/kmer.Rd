% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/seq_verbs.R
\name{kmer}
\alias{kmer}
\title{k-mer spectrum of a sequence column}
\usage{
kmer(x, seq, k = 4, by = NULL, canonical = FALSE)
}
\arguments{
\item{x}{A \code{vectra_node} object.}

\item{seq}{The sequence column (unquoted). Defaults to \code{seq}, the column
\code{\link[=tbl_fasta]{tbl_fasta()}} and \code{\link[=tbl_fastq]{tbl_fastq()}} produce.}

\item{k}{k-mer length, an integer in \code{1:32} (default 4).}

\item{by}{Grouping column(s): an unquoted name, \code{c(col1, col2)}, or a
character vector. \code{NULL} (default) counts one spectrum over the whole input.}

\item{canonical}{If \code{TRUE}, collapse each k-mer with its reverse complement
(default \code{FALSE}).}
}
\value{
A \code{vectra_node} with the grouping columns, a \code{kmer} column, and a
\code{count} column.
}
\description{
Counts every fixed-length subsequence (k-mer) of a nucleotide string column,
grouped by zero or more key columns. The result is one row per distinct
(group, k-mer): the grouping columns, a \code{kmer} column, and an integer
\code{count}. This is the set-wise counterpart to the per-row \link{seq_expressions}
family --- it materializes the whole spectrum, so it is a blocking step like
\code{\link[=summarise]{summarise()}}, but the k-mer table is the only state held, not the input.
}
\details{
Each k-mer is packed into 2 bits per base (\code{A}/\code{C}/\code{G}/\code{T}) and counted in a
native hash table, so \code{k} is limited to \code{1:32}. A window containing any
non-\code{ACGT} base (\code{N}, an IUPAC ambiguity code, or a gap) is skipped, matching
the convention of dedicated k-mer counters. With \code{canonical = TRUE}, a k-mer
and its reverse complement are counted together under the lexicographically
smaller of the two --- the usual choice for strand-agnostic data.

Row order in the result is unspecified; pipe into \code{\link[=arrange]{arrange()}} for a stable
order.
}
\examples{
f <- tempfile(fileext = ".vtr")
write_vtr(data.frame(id = c("a", "b"),
                     seq = c("ACGTACGT", "AAAAT"),
                     stringsAsFactors = FALSE), f)
tbl(f) |> kmer(seq, k = 3, by = id) |> arrange(id, kmer) |> collect()
unlink(f)

}
\seealso{
\link{seq_expressions}, \code{\link[=tbl_fasta]{tbl_fasta()}}
}
