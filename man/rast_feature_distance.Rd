% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/feature_knn.R
\name{rast_feature_distance}
\alias{rast_feature_distance}
\title{Predictor-space nearest-neighbour distance surface over a raster}
\usage{
rast_feature_distance(
  x,
  reference,
  vars = NULL,
  k = NULL,
  percentage = NULL,
  metric = c("euclidean", "mahalanobis"),
  path = NULL,
  dtype = "f32",
  nthreads = NULL,
  compression = c("fast", "balanced", "max")
)
}
\arguments{
\item{x, reference}{\code{vectra_raster} handles or paths to \code{.vec} rasters: the
raster to score and the reference raster, with matching bands (predictors)
in the same order.}

\item{vars}{Predictors to use: band names, band indices, or \code{NULL} (default,
all bands). Names resolve against \code{x}'s band names.}

\item{k}{Number of nearest reference cells to average over. Supply exactly
one of \code{k} or \code{percentage}.}

\item{percentage}{Percent of the reference cells to average over (the nearest
\code{ceil(percentage/100 * N)}). Supply exactly one of \code{k} or \code{percentage}.}

\item{metric}{\code{"euclidean"} (default) or \code{"mahalanobis"} (whitened by the
reference covariance).}

\item{path}{Optional output \code{.vec} path. When given the surface is streamed to
disk and the opened \code{\link[=vec_open_raster]{vec_open_raster()}} handle is returned invisibly; when
\code{NULL} the surface is returned as an in-memory matrix.}

\item{dtype}{Storage dtype for \code{.vec} output. Default \code{"f32"}.}

\item{nthreads}{Threads for the distance scan. \code{NULL} (default) uses all
available (capped to two under \verb{R CMD check}).}

\item{compression}{Compression effort for \code{.vec} output. Default \code{"fast"}.}
}
\value{
With \code{path = NULL}, a numeric matrix on \code{x}'s grid (row 1 northmost)
carrying \code{gt}, \code{extent}, and \code{crs} attributes. With \code{path} given, the
written single-band \code{vectra_raster} handle (invisibly). Cells with any \code{NA}
predictor come back \code{NA}.
}
\description{
Computes, aligned to the grid of a projection raster \code{x}, a surface of the
mean distance in predictor space from each cell to the nearest cells of a
reference raster \code{reference}. Both are multi-band environmental rasters with
one band per predictor. This is the raster, out-of-core counterpart of
\code{\link[=feature_knn]{feature_knn()}}: the reference raster is read once into memory and indexed,
while \code{x} is walked one tile-row strip at a time and streamed to the sink, so
the projection side is larger-than-RAM. \code{x} and \code{reference} need not share a
grid -- the output follows \code{x}.
}
\details{
The distance is the mean distance to the nearest \code{k}, or nearest
\code{percentage}\% of the reference cells (\code{ceil(percentage/100 * N)}), under a
Euclidean or Mahalanobis metric. Supply exactly one of \code{k} or \code{percentage}.

This is the streaming distance surface behind an environmental-novelty or
transferability diagnostic such as MOP (Owens et al. 2013). The strict
non-analogous-conditions layers (per-predictor out-of-range counts) are a
separate, already-native computation -- a per-band range reduce plus
\code{\link[=rast_calc]{rast_calc()}} -- and are shown alongside this surface in the
\code{vignette("sdm")}; this function is the continuous-distance piece only.

\code{x} and \code{reference} must be \code{.vec} rasters (or paths to them); bring GeoTIFFs
onto the \code{.vec} grid with \code{\link[=warp]{warp()}} first.
}
\examples{
# Two-band reference and projection rasters.
set.seed(1)
r1 <- matrix(rnorm(400, 10, 2), 20, 20)
r2 <- matrix(rnorm(400, 800, 40), 20, 20)
x1 <- r1 + 3               # projection shifted warmer/drier -> more novel
x2 <- r2 - 60
fr <- tempfile(fileext = ".vec"); fx <- tempfile(fileext = ".vec")
vec_write_raster(array(c(r1, r2), c(20, 20, 2)), fr, dtype = "f64",
                 extent = c(0, 0, 20, 20), band_names = c("bio1", "bio12"))
vec_write_raster(array(c(x1, x2), c(20, 20, 2)), fx, dtype = "f64",
                 extent = c(0, 0, 20, 20), band_names = c("bio1", "bio12"))

d <- rast_feature_distance(fx, fr, percentage = 10)
round(mean(d), 2)
unlink(c(fr, fx))

}
\references{
Owens, H.L. et al. (2013) Constraints on interpretation of
ecological niche models by limited environmental ranges on calibration
areas. \emph{Ecological Modelling} 263:10-18.
}
\seealso{
\code{\link[=feature_knn]{feature_knn()}} for the table-level primitive, \code{\link[=warp]{warp()}} to bring
rasters onto a shared grid, \code{\link[=rast_calc]{rast_calc()}} for cellwise raster algebra.
}
