% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/seq_expr.R
\name{seq_expressions}
\alias{seq_expressions}
\title{Biological-sequence functions inside mutate(), filter(), and summarise()}
\description{
vectra recognizes a family of \verb{seq_*} functions directly inside expression
verbs. A DNA, RNA, or protein sequence rides through the engine as an
ordinary ASCII string column; these functions decode it in C one row at a
time and compute a measure, a transformed sequence, or an edit distance
straight off it, streaming at engine speed with no round-trip through
\pkg{Biostrings}. \code{tbl_fasta("reads.fa") |> mutate(gc = seq_gc(seq))} adds a
GC-content column; \code{filter(seq_dist(seq, ref) <= 3)} prunes the stream in C.
}
\details{
These names are interpreted by the expression engine; they are not exported R
functions and are not called as such. They are available only inside
\code{\link[=mutate]{mutate()}}, \code{\link[=transmute]{transmute()}}, \code{\link[=filter]{filter()}}, and a grouped \code{\link[=summarise]{summarise()}} over a
\code{vectra_node}. The sequence argument is an ordinary string column.
}
\section{Measures}{

\describe{
\item{\code{seq_length(x)}}{Number of characters in the sequence (integer).}
\item{\code{seq_gc(x)}}{GC fraction: the count of \code{G} and \code{C} divided by the
sequence length, in \verb{[0, 1]} (double). Case-insensitive; only literal
\code{G}/\code{C} are counted, so an ambiguity code such as \code{S} does not contribute.}
}
}

\section{Transforms (return a sequence)}{

\describe{
\item{\code{seq_revcomp(x)}}{Reverse complement.}
\item{\code{seq_complement(x)}}{Complement, DNA alphabet (\code{A}<->\code{T}), with the
IUPAC ambiguity codes mapped to their complements and case preserved. A
\code{U} is complemented to \code{A}; transcribe first for an RNA-alphabet
complement.}
\item{\code{seq_reverse(x)}}{Reverse the sequence (no complement).}
\item{\code{seq_transcribe(x)}}{Swap \code{T}<->\code{U}, turning DNA into RNA and RNA into
DNA (case preserved).}
\item{\code{seq_translate(x, table = 1)}}{Translate in reading frame 1 to a
protein sequence using the standard genetic code (NCBI \code{transl_table} 1;
\code{table} currently accepts only 1). A trailing partial codon is dropped; a
codon containing any non-\code{ACGTU} base yields \code{X}, a stop codon \code{*}.}
\item{\code{seq_subseq(x, start, width)}}{Substring of \code{width} characters
starting at 1-based \code{start}, clamped to the sequence. \code{start} and \code{width}
may be columns or constants.}
}
}

\section{Distance}{

\describe{
\item{\code{seq_dist(x, ref, method = "levenshtein")}}{Edit distance between each
sequence and a reference (integer). \code{ref} is another sequence column
(compared row by row) or a single constant string (compared against every
row). \code{method} is \code{"levenshtein"} (default; also \code{"lv"}, \code{"edit"}),
\code{"dl"} / \code{"damerau"} / \code{"osa"} for the optimal-string-alignment
Damerau-Levenshtein distance, or \code{"hamming"} (substitutions only; \code{NA}
when the two sequences differ in length).}
}
}

\section{Missing sequence}{

A missing (\code{NA}) cell yields \code{NA} for that row rather than an error, matching
the \verb{st_*} and embedding-distance contract. Unexpected characters are
processed as-is (passed through by complement, translated to \code{X}).
}

\examples{
reads <- data.frame(
  id  = c("r1", "r2", "r3"),
  seq = c("ATGGCCATTGTA", "GGGCCCTTTAAA", "ATGTAA"),
  stringsAsFactors = FALSE
)
f <- tempfile(fileext = ".vtr")
write_vtr(reads, f)

tbl(f) |>
  mutate(
    len = seq_length(seq),
    gc  = seq_gc(seq),
    rc  = seq_revcomp(seq),
    aa  = seq_translate(seq),
    d   = seq_dist(seq, "ATGGCCATTGTA")
  ) |>
  collect()

unlink(f)
}
\seealso{
\code{\link[=mutate]{mutate()}}, \code{\link[=filter]{filter()}}; \link{geom_expressions} and the embedding
distances (\code{cosine}, \code{l2}, \code{dot}) for the other per-row blob-column
families.
}
