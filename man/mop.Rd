% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/feature_knn.R
\name{mop}
\alias{mop}
\title{MOP transferability / novelty surface between two environmental rasters}
\usage{
mop(
  g,
  m,
  vars = NULL,
  percentage = 10,
  k = NULL,
  metric = c("euclidean", "mahalanobis"),
  path = NULL,
  dtype = "f32",
  nthreads = NULL,
  compression = c("fast", "balanced", "max")
)
}
\arguments{
\item{g, m}{\code{vectra_raster} handles or paths to \code{.vec} rasters: the projection
region and the calibration region, with matching bands (predictors) in the
same order.}

\item{vars}{Predictors to use: band names, band indices, or \code{NULL} (default,
all bands). Names resolve against \code{g}'s band names.}

\item{percentage}{Percent of the calibration cloud averaged over for the
distance surface. Default \code{10}.}

\item{k}{Absolute neighbour count for the distance surface. When given it
overrides \code{percentage}.}

\item{metric}{\code{"euclidean"} (default) or \code{"mahalanobis"} (whitened by the
calibration covariance).}

\item{path}{Optional output \code{.vec} path. When given the five-band result is
streamed to disk and the opened \code{\link[=vec_open_raster]{vec_open_raster()}} handle is returned
invisibly; when \code{NULL} a named list of five in-memory matrices is returned.}

\item{dtype}{Storage dtype for \code{.vec} output. Default \code{"f32"}.}

\item{nthreads}{Threads for the distance scan. \code{NULL} (default) uses all
available (capped to two under \verb{R CMD check}).}

\item{compression}{Compression effort for \code{.vec} output. Default \code{"fast"}.}
}
\value{
With \code{path = NULL}, a named list of five numeric matrices
(\code{mop_distance}, \code{mop_basic}, \code{mop_simple}, \code{towards_low}, \code{towards_high}),
each carrying \code{gt}, \code{extent}, and \code{crs} attributes and aligned to \code{g}. With
\code{path} given, the written five-band \code{vectra_raster} handle (invisibly).
}
\description{
Computes the mobility-oriented parity (MOP) diagnostic of Owens et al. (2013)
between a projection raster \code{g} and a calibration raster \code{m}, both multi-band
environmental layers with one band per predictor. It returns, aligned to the
projection grid, both halves of \code{mop::mop(type = "detailed")}:
}
\details{
\itemize{
\item \code{mop_distance} -- the continuous MOP surface: per \code{g} cell, the mean
distance in predictor space to the nearest \code{percentage}\% (or nearest \code{k}) of
the \code{m} cells, via \code{\link[=feature_knn]{feature_knn()}}.
\item The non-analogous-conditions (NAC) / strict-extrapolation layers, per cell:
\code{towards_low} (predictors below their calibration minimum), \code{towards_high}
(above their calibration maximum), \code{mop_simple} (their sum, the count of
out-of-range predictors), and \code{mop_basic} (1 where any predictor is out of
range, 0 otherwise).
}

The calibration cloud \code{m} is read once into memory and indexed; the projection
raster \code{g} is walked one tile-row strip at a time and streamed to the sink, so
the projection side is out-of-core. \code{g} and \code{m} need not share a grid -- the
output follows \code{g}. When both \code{k} and \code{percentage} are given, \code{k} wins.

\code{g} and \code{m} must be \code{.vec} rasters (or paths to them); bring GeoTIFFs onto the
\code{.vec} grid with \code{\link[=warp]{warp()}} first.
}
\examples{
# Two-band calibration (m) and projection (g) rasters on a shared grid.
set.seed(1)
m1 <- matrix(rnorm(400, 10, 2), 20, 20)
m2 <- matrix(rnorm(400, 800, 40), 20, 20)
g1 <- m1 + 3               # projection shifted warmer/drier -> more novel
g2 <- m2 - 60
fm <- tempfile(fileext = ".vec"); fg <- tempfile(fileext = ".vec")
vec_write_raster(array(c(m1, m2), c(20, 20, 2)), fm, dtype = "f64",
                 extent = c(0, 0, 20, 20), band_names = c("bio1", "bio12"))
vec_write_raster(array(c(g1, g2), c(20, 20, 2)), fg, dtype = "f64",
                 extent = c(0, 0, 20, 20), band_names = c("bio1", "bio12"))

out <- mop(fg, fm, percentage = 10)
names(out)
round(mean(out$mop_distance), 2)
unlink(c(fm, fg))

}
\references{
Owens, H.L. et al. (2013) Constraints on interpretation of
ecological niche models by limited environmental ranges on calibration
areas. \emph{Ecological Modelling} 263:10-18.
}
\seealso{
\code{\link[=feature_knn]{feature_knn()}} for the underlying predictor-space kNN, \code{\link[=warp]{warp()}} to
bring rasters onto a shared grid, \code{\link[=rast_calc]{rast_calc()}} for cellwise raster algebra.
}
